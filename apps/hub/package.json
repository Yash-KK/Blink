{
  "name": "hub",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot run index.ts",
    "build": "bun build index.ts --outdir dist --target bun"
  },
  "devDependencies": {
    "@repo/db": "*",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
